## Детали реализации

Сборка приложения:
`docker compose build
`

Запуск приложения:
`docker compose up -d
`

Образ keycloak заменен на другой, в котором есть поддержка российский провайдеров.
Исходный образ не удалось настроить для работы с Яндекс OAuth через коннектор OpenID Connect.